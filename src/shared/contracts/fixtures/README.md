# Contract fixtures

`org-plan-attention-contract.v1.json` mirrors the schema-v1 fixture published by
`dyne/gestalt-agents` at
`tests/plugins/gestalt/fixtures/org-plan-attention-contract.json`, commit
`cce12d9`. Mobile tests use this portable copy; production code never reads a
sibling checkout.
